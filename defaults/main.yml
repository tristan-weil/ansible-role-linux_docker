---

docker_dir: /var/lib/docker
docker_force_dir: False

# docker_version:

docker_daemon_enabled: True

docker_firewall_config_ipv4_rules: |
  -N DOCKER
  -N DOCKER-ISOLATION-STAGE-1
  -N DOCKER-ISOLATION-STAGE-2
  -N DOCKER-USER
  -A FORWARD -j DOCKER-USER
  -A FORWARD -j DOCKER-ISOLATION-STAGE-1
  -A FORWARD -o br-10715f377e43 -j DOCKER
  -A FORWARD -o docker0 -m conntrack --ctstate RELATED,ESTABLISHED -j ACCEPT
  -A FORWARD -o docker0 -j DOCKER
  -A FORWARD -i docker0 ! -o docker0 -j ACCEPT
  -A FORWARD -i docker0 -o docker0 -j ACCEPT
  -A DOCKER -d 172.19.0.8/32 ! -i br-10715f377e43 -o br-10715f377e43 -p tcp -m tcp --dport 8443 -j ACCEPT
  -A DOCKER-ISOLATION-STAGE-1 -i br-10715f377e43 ! -o br-10715f377e43 -j DOCKER-ISOLATION-STAGE-2
  -A DOCKER-ISOLATION-STAGE-1 -i docker0 ! -o docker0 -j DOCKER-ISOLATION-STAGE-2
  -A DOCKER-ISOLATION-STAGE-1 -j RETURN
  -A DOCKER-ISOLATION-STAGE-2 -o br-10715f377e43 -j DROP
  -A DOCKER-ISOLATION-STAGE-2 -o docker0 -j DROP
  -A DOCKER-ISOLATION-STAGE-2 -j RETURN
  -A DOCKER-USER -j RETURN

docker_firewall_config_ipv6_rules: ''
